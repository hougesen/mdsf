name: test
on:
  - push
  - workflow_dispatch
concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.run_id }}
  cancel-in-progress: true
env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: full
jobs:
  tools:
    name: tools
    strategy:
      matrix:
        os:
          - ubuntu-latest
    runs-on: ${{ matrix.os }}
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v6
      - uses: Swatinem/rust-cache@v2
      - uses: erlef/setup-beam@v1
        with:
          elixir-version: 1.16.1
          gleam-version: 1.0.0
          otp-version: "26"
      - uses: actions/setup-python@v6
        with:
          cache: pip
      - uses: actions/setup-node@v6
        with:
          node-version: "24"
      - uses: pnpm/action-setup@v4
        with:
          run_install: false
          version: latest
      - run: |
          echo "STORE_PATH=$(pnpm store path --silent)" >> $GITHUB_ENV
      - uses: actions/cache@v5
        with:
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles('**/pnpm-lock.yaml') }}
          path: ${{ env.STORE_PATH }}
          restore-keys: |
            ${{ runner.os }}-pnpm-store-
      - uses: jiro4989/setup-nim-action@v2
      - uses: mlugg/setup-zig@v2
      - uses: uncenter/setup-taplo@v1
      - uses: actions/setup-go@v6
        with:
          go-version: stable
      - uses: ruby/setup-ruby@v1
        with:
          ruby-version: "3.3"
      - uses: dart-lang/setup-dart@v1
      - uses: crystal-lang/install-crystal@v1
      - uses: hasnep/setup-roc@v0.5.0
      - uses: denoland/setup-deno@v2
      - uses: taiki-e/install-action@just
      - uses: ocaml/setup-ocaml@v3
        with:
          dune-cache: true
          ocaml-compiler: "5.1"
      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "21"
      - uses: haskell-actions/setup@v2
      - uses: julia-actions/cache@v2
      - uses: cargo-bins/cargo-binstall@main
      - uses: Homebrew/actions/setup-homebrew@master
      - uses: leafo/gh-actions-lua@v12
      - uses: leafo/gh-actions-luarocks@v6
        continue-on-error: true
      - uses: dlang-community/setup-dlang@v2
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest
      - uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true
      - uses: jdx/mise-action@v3
      - name: Install cargo-llvm-cov
        uses: taiki-e/install-action@cargo-llvm-cov
      - run: rustup toolchain install stable
      - run: rustup component add rustfmt clippy
      - run: curl -LsSf https://github.com/posit-dev/air/releases/latest/download/air-installer.sh | sh
      - run: which actionlint || ( which mise && mise use -g actionlint )
        continue-on-error: true
      - run: which actionlint || ( which brew && brew install actionlint )
        continue-on-error: true
      - run: which actionlint || ( which go && go install github.com/rhysd/actionlint/cmd/actionlint@latest )
        continue-on-error: true
      - run: which air || ( which brew && brew install air )
        continue-on-error: true
      - run: which alejandra || ( which cargo && ( cargo binstall alejandra || cargo install alejandra ) )
        continue-on-error: true
      - run: which alex || ( which npm && npm i -g alex )
        continue-on-error: true
      - run: which auto-optional || ( which pipx && pipx install auto-optional )
        continue-on-error: true
      - run: which autoflake || ( which pipx && pipx install autoflake )
        continue-on-error: true
      - run: which autopep8 || ( which pipx && pipx install autopep8 )
        continue-on-error: true
      - run: which bashate || ( which brew && brew install bashate )
        continue-on-error: true
      - run: which bashate || ( which pipx && pipx install bashate )
        continue-on-error: true
      - run: which beautysh || ( which brew && brew install beautysh )
        continue-on-error: true
      - run: which biome || ( which mise && mise use -g biome )
        continue-on-error: true
      - run: which biome || ( which npm && npm i -g @biomejs/biome )
        continue-on-error: true
      - run: which biome || ( which brew && brew install biome )
        continue-on-error: true
      - run: which black || ( which brew && brew install black )
        continue-on-error: true
      - run: which black || ( which pipx && pipx install black )
        continue-on-error: true
      - run: which blade-formatter || ( which npm && npm i -g blade-formatter )
        continue-on-error: true
      - run: which blue || ( which pipx && pipx install blue )
        continue-on-error: true
      - run: which brunette || ( which pipx && pipx install brunette )
        continue-on-error: true
      - run: which buf || ( which mise && mise use -g buf )
        continue-on-error: true
      - run: which buf || ( which npm && npm i -g @bufbuild/buf )
        continue-on-error: true
      - run: which buf || ( which brew && brew install bufbuild/buf/buf )
        continue-on-error: true
      - run: which clang-format || ( which mise && mise use -g clang-format )
        continue-on-error: true
      - run: which clang-format || ( which brew && brew install clang-format )
        continue-on-error: true
      - run: which clang-format || ( which pipx && pipx install clang-format )
        continue-on-error: true
      - run: which coffeelint || ( which npm && npm i -g @coffeelint/cli )
        continue-on-error: true
      - run: which cppcheck || ( which brew && brew install cppcheck )
        continue-on-error: true
      - run: which cppcheck || ( which apt-get && sudo apt-get install -y cppcheck )
        continue-on-error: true
      - run: which cpplint || ( which brew && brew install cpplint )
        continue-on-error: true
      - run: which cpplint || ( which apt-get && sudo apt-get install -y cpplint )
        continue-on-error: true
      - run: which cpplint || ( which pipx && pipx install cpplint )
        continue-on-error: true
      - run: which crystal || ( which mise && mise use -g crystal )
        continue-on-error: true
      - run: which csharpier || ( which dotnet && dotnet tool install -g csharpier )
        continue-on-error: true
      - run: which css-beautify || ( which npm && npm i -g js-beautify )
        continue-on-error: true
      - run: which csscomb || ( which npm && npm i -g csscomb )
        continue-on-error: true
      - run: which csslint || ( which npm && npm i -g csslint )
        continue-on-error: true
      - run: which dart || ( which mise && mise use -g dart )
        continue-on-error: true
      - run: which deno || ( which mise && mise use -g deno )
        continue-on-error: true
      - run: which deno || ( which cargo && ( cargo binstall deno || cargo install deno ) )
        continue-on-error: true
      - run: which deno || ( which brew && brew install deno )
        continue-on-error: true
      - run: which dfmt || ( which brew && brew install dfmt )
        continue-on-error: true
      - run: which dockerfmt || ( which go && go install github.com/reteps/dockerfmt@latest )
        continue-on-error: true
      - run: which dune || ( which opam && eval $(opam env) && opam install dune )
        continue-on-error: true
      - run: which dx || ( which cargo && ( cargo binstall dioxus-cli || cargo install dioxus-cli ) )
        continue-on-error: true
      - run: which efmt || ( which cargo && ( cargo binstall efmt || cargo install efmt ) )
        continue-on-error: true
      - run: which elm-format || ( which npm && npm i -g elm-format )
        continue-on-error: true
      - run: which erg || ( which cargo && ( cargo binstall erg || cargo install erg ) )
        continue-on-error: true
      - run: which fantomas || ( which dotnet && dotnet tool install -g fantomas )
        continue-on-error: true
      - run: which fixjson || ( which npm && npm i -g fixjson )
        continue-on-error: true
      - run: which fortitude || ( which brew && brew install fortitude )
        continue-on-error: true
      - run: which fortitude || ( which pipx && pipx install fortitude-lint )
        continue-on-error: true
      - run: which fortran-linter || ( which pipx && pipx install fortran-linter )
        continue-on-error: true
      - run: which fourmolu || ( which brew && brew install fourmolu )
        continue-on-error: true
      - run: which fourmolu || ( which stack && stack install fourmolu )
        continue-on-error: true
      - run: which fourmolu || ( which cabal && cabal install fourmolu )
        continue-on-error: true
      - run: which fprettify || ( which brew && brew install fprettify )
        continue-on-error: true
      - run: which fprettify || ( which pipx && pipx install fprettify )
        continue-on-error: true
      - run: which gleam || ( which mise && mise use -g gleam )
        continue-on-error: true
      - run: which gofumpt || ( which mise && mise use -g gofumpt )
        continue-on-error: true
      - run: which gofumpt || ( which go && go install mvdan.cc/gofumpt@latest )
        continue-on-error: true
      - run: which goimports || ( which go && go install golang.org/x/tools/cmd/goimports@latest )
        continue-on-error: true
      - run: which golines || ( which go && go install github.com/segmentio/golines@latest )
        continue-on-error: true
      - run: which google-java-format || ( which brew && brew install google-java-format )
        continue-on-error: true
      - run: which grain || ( which mise && mise use -g grain )
        continue-on-error: true
      - run: which hindent || ( which apt-get && sudo apt-get install -y hindent )
        continue-on-error: true
      - run: which hindent || ( which stack && stack install hindent )
        continue-on-error: true
      - run: which hlint || ( which brew && brew install hlint )
        continue-on-error: true
      - run: which hlint || ( which stack && stack install hlint )
        continue-on-error: true
      - run: which hlint || ( which cabal && cabal install hlint )
        continue-on-error: true
      - run: which html-beautify || ( which npm && npm i -g js-beautify )
        continue-on-error: true
      - run: which htmlbeautifier || ( which gem && gem install htmlbeautifier )
        continue-on-error: true
      - run: which hurlfmt || ( which cargo && ( cargo binstall hurlfmt || cargo install hurlfmt ) )
        continue-on-error: true
      - run: which isort || ( which pipx && pipx install isort )
        continue-on-error: true
      - run: which jq || ( which mise && mise use -g jq )
        continue-on-error: true
      - run: which jq || ( which apt-get && sudo apt-get install -y jq )
        continue-on-error: true
      - run: which jqfmt || ( which go && go install github.com/noperator/jqfmt/cmd/jqfmt@latest )
        continue-on-error: true
      - run: which js-beautify || ( which npm && npm i -g js-beautify )
        continue-on-error: true
      - run: which json_repair || ( which pipx && pipx install json-repair )
        continue-on-error: true
      - run: which jsonlint || ( which npm && npm i -g jsonlint )
        continue-on-error: true
      - run: which jsonpp || ( which brew && brew install jsonpp )
        continue-on-error: true
      - run: which juliaformatter.jl || ( which julia && julia  -e 'import Pkg; Pkg.add("JuliaFormatter")' )
        continue-on-error: true
      - run: which just || ( which mise && mise use -g just )
        continue-on-error: true
      - run: which just || ( which npm && npm i -g rust-just )
        continue-on-error: true
      - run: which just || ( which cargo && ( cargo binstall just || cargo install just ) )
        continue-on-error: true
      - run: which just || ( which brew && brew install just )
        continue-on-error: true
      - run: which just || ( which pipx && pipx install rust-just )
        continue-on-error: true
      - run: which kcl || ( which brew && brew install kcl-lang/tap/kcl )
        continue-on-error: true
      - run: which kcl || ( which go && go install kcl-lang.io/cli/cmd/kcl@latest )
        continue-on-error: true
      - run: which kdlfmt || ( which npm && npm i -g kdlfmt )
        continue-on-error: true
      - run: which kdlfmt || ( which cargo && ( cargo binstall kdlfmt || cargo install kdlfmt ) )
        continue-on-error: true
      - run: which kdlfmt || ( which brew && brew install hougesen/tap/kdlfmt )
        continue-on-error: true
      - run: which ktfmt || ( which brew && brew install ktfmt )
        continue-on-error: true
      - run: which ktlint || ( which mise && mise use -g ktlint )
        continue-on-error: true
      - run: which ktlint || ( which brew && brew install ktlint )
        continue-on-error: true
      - run: which lua-format || ( which luarocks && luarocks install luaformatter )
        continue-on-error: true
      - run: which mado || ( which brew && brew tap akiomik/mado https://github.com/akiomik/mado.git && brew install mado )
        continue-on-error: true
      - run: which mago || ( which cargo && ( cargo binstall mago || cargo install mago ) )
        continue-on-error: true
      - run: which mago || ( which brew && brew install mago )
        continue-on-error: true
      - run: which mago || ( which composer && composer require carthage-software/mago )
        continue-on-error: true
      - run: which markdownfmt || ( which go && go install github.com/shurcooL/markdownfmt@latest )
        continue-on-error: true
      - run: which markdownlint || ( which npm && npm i -g markdownlint-cli )
        continue-on-error: true
      - run: which markdownlint-cli2 || ( which mise && mise use -g markdownlint-cli2 )
        continue-on-error: true
      - run: which markdownlint-cli2 || ( which npm && npm i -g markdownlint-cli2 )
        continue-on-error: true
      - run: which markdownlint-cli2 || ( which brew && brew install markdownlint-cli2 )
        continue-on-error: true
      - run: which mbake || ( which pipx && pipx install mbake )
        continue-on-error: true
      - run: which mdsf || ( which npm && npm i -g mdsf-cli )
        continue-on-error: true
      - run: which mdsf || ( which cargo && ( cargo binstall mdsf || cargo install mdsf ) )
        continue-on-error: true
      - run: which mdsf || ( which brew && brew install hougesen/tap/mdsf )
        continue-on-error: true
      - run: which mise || ( which npm && npm i -g @jdxcode/mise )
        continue-on-error: true
      - run: which mise || ( which cargo && ( cargo binstall mise || cargo install mise ) )
        continue-on-error: true
      - run: which mise || ( which brew && brew install mise )
        continue-on-error: true
      - run: which misspell || ( which go && go install github.com/client9/misspell/cmd/misspell@latest )
        continue-on-error: true
      - run: which nginxbeautifier || ( which npm && npm i -g nginxbeautifier )
        continue-on-error: true
      - run: which nginxfmt || ( which pipx && pipx install nginxfmt )
        continue-on-error: true
      - run: which nixpkgs-fmt || ( which cargo && ( cargo binstall nixpkgs-fmt || cargo install nixpkgs-fmt ) )
        continue-on-error: true
      - run: which nph || ( which nimble && nimble install nph )
        continue-on-error: true
      - run: which ocamlformat || ( which opam && eval $(opam env) && opam install ocamlformat )
        continue-on-error: true
      - run: which ocp-indent || ( which apt-get && sudo apt-get install -y ocp-indent )
        continue-on-error: true
      - run: which ocp-indent || ( which opam && eval $(opam env) && opam install ocp-indent )
        continue-on-error: true
      - run: which ormolu || ( which brew && brew install ormolu )
        continue-on-error: true
      - run: which ormolu || ( which apt-get && sudo apt-get install -y ormolu )
        continue-on-error: true
      - run: which ormolu || ( which stack && stack install ormolu )
        continue-on-error: true
      - run: which ormolu || ( which cabal && cabal install ormolu )
        continue-on-error: true
      - run: which oxfmt || ( which npm && npm i -g oxfmt )
        continue-on-error: true
      - run: which oxlint || ( which mise && mise use -g oxlint )
        continue-on-error: true
      - run: which oxlint || ( which npm && npm i -g oxlint )
        continue-on-error: true
      - run: which perflint || ( which pipx && pipx install perflint )
        continue-on-error: true
      - run: which prettier || ( which npm && npm i -g prettier )
        continue-on-error: true
      - run: which prettierd || ( which npm && npm i -g @fsouza/prettierd )
        continue-on-error: true
      - run: which prettierd || ( which brew && brew install prettierd )
        continue-on-error: true
      - run: which purs-tidy || ( which npm && npm i -g purs-tidy )
        continue-on-error: true
      - run: which purty || ( which npm && npm i -g purty )
        continue-on-error: true
      - run: which pycln || ( which pipx && pipx install pycln )
        continue-on-error: true
      - run: which pyflakes || ( which pipx && pipx install pyflakes )
        continue-on-error: true
      - run: which pyink || ( which pipx && pipx install pyink )
        continue-on-error: true
      - run: which pylint || ( which pipx && pipx install pylint )
        continue-on-error: true
      - run: which pymarkdownlnt || ( which pipx && pipx install pymarkdownlnt )
        continue-on-error: true
      - run: which pyrefly || ( which pipx && pipx install pyrefly )
        continue-on-error: true
      - run: which pyupgrade || ( which brew && brew install pyupgrade )
        continue-on-error: true
      - run: which pyupgrade || ( which pipx && pipx install pyupgrade )
        continue-on-error: true
      - run: which reorder-python-imports || ( which brew && brew install reorder-python-imports )
        continue-on-error: true
      - run: which reorder-python-imports || ( which pipx && pipx install reorder-python-imports )
        continue-on-error: true
      - run: which rescript || ( which npm && npm i -g rescript )
        continue-on-error: true
      - run: which rubocop || ( which gem && gem install rubocop )
        continue-on-error: true
      - run: which rubyfmt || ( which brew && brew install rubyfmt )
        continue-on-error: true
      - run: which ruff || ( which mise && mise use -g ruff )
        continue-on-error: true
      - run: which ruff || ( which brew && brew install ruff )
        continue-on-error: true
      - run: which ruff || ( which pipx && pipx install ruff )
        continue-on-error: true
      - run: which rufo || ( which gem && gem install rufo )
        continue-on-error: true
      - run: which rustywind || ( which npm && npm i -g rustywind )
        continue-on-error: true
      - run: which rustywind || ( which cargo && ( cargo binstall rustywind || cargo install rustywind ) )
        continue-on-error: true
      - run: which scalafmt || ( which cs && cs install scalafmt )
        continue-on-error: true
      - run: which selene || ( which cargo && ( cargo binstall selene || cargo install selene ) )
        continue-on-error: true
      - run: which semistandard || ( which npm && npm i -g semistandard )
        continue-on-error: true
      - run: which shellcheck || ( which mise && mise use -g shellcheck )
        continue-on-error: true
      - run: which shellcheck || ( which brew && brew install shellcheck )
        continue-on-error: true
      - run: which shellcheck || ( which apt-get && sudo apt-get install -y shellcheck )
        continue-on-error: true
      - run: which shellcheck || ( which stack && stack install ShellCheck )
        continue-on-error: true
      - run: which shellcheck || ( which cabal && cabal install ShellCheck )
        continue-on-error: true
      - run: which shfmt || ( which mise && mise use -g shfmt )
        continue-on-error: true
      - run: which shfmt || ( which brew && brew install shfmt )
        continue-on-error: true
      - run: which shfmt || ( which go && go install mvdan.cc/sh/v3/cmd/shfmt@latest )
        continue-on-error: true
      - run: which sleek || ( which cargo && ( cargo binstall sleek || cargo install sleek ) )
        continue-on-error: true
      - run: which sleek || ( which brew && brew install sleek )
        continue-on-error: true
      - run: which smlfmt || ( which brew && brew install smlfmt )
        continue-on-error: true
      - run: which snakefmt || ( which brew && brew install snakefmt )
        continue-on-error: true
      - run: which snakefmt || ( which pipx && pipx install snakefmt )
        continue-on-error: true
      - run: which sql-formatter || ( which npm && npm i -g sql-formatter )
        continue-on-error: true
      - run: which sqlfluff || ( which pipx && pipx install sqlfluff )
        continue-on-error: true
      - run: which sqlfmt || ( which pipx && pipx install shandy-sqlfmt[jinjafmt] )
        continue-on-error: true
      - run: which sqlint || ( which gem && gem install sqlint )
        continue-on-error: true
      - run: which sqruff || ( which cargo && ( cargo binstall sqruff || cargo install sqruff ) )
        continue-on-error: true
      - run: which sqruff || ( which pipx && pipx install sqruff )
        continue-on-error: true
      - run: which squawk || ( which npm && npm i -g squawk-cli )
        continue-on-error: true
      - run: which squawk || ( which pipx && pipx install squawk-cli )
        continue-on-error: true
      - run: which standard || ( which npm && npm i -g standard )
        continue-on-error: true
      - run: which standardrb || ( which gem && gem install standardrb )
        continue-on-error: true
      - run: which stylefmt || ( which npm && npm i -g stylefmt )
        continue-on-error: true
      - run: which stylish-haskell || ( which brew && brew install stylish-haskell )
        continue-on-error: true
      - run: which stylish-haskell || ( which stack && stack install stylish-haskell )
        continue-on-error: true
      - run: which stylish-haskell || ( which cabal && cabal install stylish-haskell )
        continue-on-error: true
      - run: which stylua || ( which mise && mise use -g stylua )
        continue-on-error: true
      - run: which stylua || ( which npm && npm i -g @johnnymorganz/stylua-bin )
        continue-on-error: true
      - run: which stylua || ( which cargo && ( cargo binstall stylua || cargo install stylua ) )
        continue-on-error: true
      - run: which stylua || ( which brew && brew install stylua )
        continue-on-error: true
      - run: which superhtml || ( which mise && mise use -g superhtml )
        continue-on-error: true
      - run: which superhtml || ( which brew && brew install superhtml )
        continue-on-error: true
      - run: which swiftformat || ( which mise && mise use -g swiftformat )
        continue-on-error: true
      - run: which swiftformat || ( which brew && brew install swiftformat )
        continue-on-error: true
      - run: which taplo || ( which mise && mise use -g taplo )
        continue-on-error: true
      - run: which taplo || ( which npm && npm i -g @taplo/cli )
        continue-on-error: true
      - run: which taplo || ( which cargo && ( cargo binstall taplo-cli || cargo install taplo-cli ) )
        continue-on-error: true
      - run: which taplo || ( which brew && brew install taplo )
        continue-on-error: true
      - run: which terraform || ( which brew && brew tap hashicorp/tap && brew install hashicorp/tap/terraform )
        continue-on-error: true
      - run: which tex-fmt || ( which cargo && ( cargo binstall tex-fmt || cargo install tex-fmt ) )
        continue-on-error: true
      - run: which tex-fmt || ( which brew && brew install tex-fmt )
        continue-on-error: true
      - run: which tex-fmt || ( which apt-get && sudo apt-get install -y tex-fmt )
        continue-on-error: true
      - run: which tofu || ( which brew && brew install opentofu )
        continue-on-error: true
      - run: which tombi || ( which mise && mise use -g tombi )
        continue-on-error: true
      - run: which tombi || ( which pipx && pipx install tombi )
        continue-on-error: true
      - run: which toml-sort || ( which pipx && pipx install toml-sort )
        continue-on-error: true
      - run: which topiary || ( which cargo && ( cargo binstall topiary-cli || cargo install topiary-cli ) )
        continue-on-error: true
      - run: which tsp || ( which npm && npm i -g @typespec/compiler )
        continue-on-error: true
      - run: which ty || ( which pipx && pipx install ty )
        continue-on-error: true
      - run: which ufmt || ( which pipx && pipx install ufmt )
        continue-on-error: true
      - run: which uiua || ( which cargo && ( cargo binstall uiua || cargo install uiua ) )
        continue-on-error: true
      - run: which unimport || ( which pipx && pipx install unimport )
        continue-on-error: true
      - run: which usort || ( which pipx && pipx install usort )
        continue-on-error: true
      - run: which verusfmt || ( which cargo && ( cargo binstall verusfmt || cargo install verusfmt ) )
        continue-on-error: true
      - run: which vsg || ( which pipx && pipx install vsg )
        continue-on-error: true
      - run: which wfindent || ( which brew && brew install findent )
        continue-on-error: true
      - run: which wfindent || ( which apt-get && sudo apt-get install -y findent )
        continue-on-error: true
      - run: which xmlformat || ( which pipx && pipx install xmlformatter )
        continue-on-error: true
      - run: which xmllint || ( which apt-get && sudo apt-get install -y libxml2-utils )
        continue-on-error: true
      - run: which xo || ( which npm && npm i -g xo )
        continue-on-error: true
      - run: which xq || ( which brew && brew install xq )
        continue-on-error: true
      - run: which xq || ( which apt-get && sudo apt-get install -y xq )
        continue-on-error: true
      - run: which yamlfix || ( which brew && brew install yamlfix )
        continue-on-error: true
      - run: which yamlfix || ( which pipx && pipx install yamlfix )
        continue-on-error: true
      - run: which yamlfmt || ( which mise && mise use -g yamlfmt )
        continue-on-error: true
      - run: which yamlfmt || ( which brew && brew install yamlfmt )
        continue-on-error: true
      - run: which yamlfmt || ( which go && go install github.com/google/yamlfmt/cmd/yamlfmt@latest )
        continue-on-error: true
      - run: which yamllint || ( which brew && brew install yamllint )
        continue-on-error: true
      - run: which yamllint || ( which pipx && pipx install yamllint )
        continue-on-error: true
      - run: which yapf || ( which brew && brew install yapf )
        continue-on-error: true
      - run: which yapf || ( which pipx && pipx install yapf )
        continue-on-error: true
      - run: which yew-fmt || ( which cargo && ( cargo binstall yew-fmt || cargo install yew-fmt ) )
        continue-on-error: true
      - run: which yq || ( which mise && mise use -g yq )
        continue-on-error: true
      - run: which yq || ( which brew && brew install yq )
        continue-on-error: true
      - run: which yq || ( which apt-get && sudo apt-get install -y yq )
        continue-on-error: true
      - run: which yq || ( which go && go install github.com/mikefarah/yq/v4@latest )
        continue-on-error: true
      - run: which zig || ( which mise && mise use -g zig )
        continue-on-error: true
      - run: hash -r
      - name: Run tests
        run: cargo test
      - name: Generate code coverage
        run: cargo llvm-cov --all-features --all-targets --workspace --codecov --output-path codecov.json
      - name: Upload code coverage
        uses: codecov/codecov-action@v5
        with:
          files: codecov.json
          slug: hougesen/mdsf
          token: ${{ secrets.CODECOV_TOKEN }}
